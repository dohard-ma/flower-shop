<view class="container" style="--navbar-height: {{navbarHeight + 11}}rpx;">
    <!-- 自定义导航栏 -->
    <view class="custom-navbar">
        <view class="navbar-title">花涧礼</view>
    </view>
    <!-- 搜索框 -->
    <view class="search-container">
        <t-search value="{{searchValue}}" placeholder="请输入商品名称" left-icon="search" right-icon="" bind:change="onSearchChange" class="search-input" />
        <view class="search-icons">
            <t-icon name="qrcode" size="48rpx" class="search-icon" />
            <t-icon name="app" size="48rpx" class="search-icon" />
        </view>
    </view>
    <!-- 轮播图占位 -->
    <view class="swiper-container">
        <t-swiper current="{{currentSwiperIndex}}" autoplay="{{true}}" interval="3000" duration="500" height="400" bind:change="onSwiperChange" list="{{swiperList}}"></t-swiper>
    </view>
    <!-- 功能按钮占位 -->
    <view class="feature-buttons">
        <view class="feature-item">
            <t-icon name="flower" size="64rpx" />
            <text>订花</text>
        </view>
        <view class="feature-item">
            <t-icon name="shop" size="64rpx" />
            <text>周边商城</text>
        </view>
    </view>
    <!-- 功能按钮占位 -->
    <view class="feature-buttons">
        <view class="feature-item">
            <t-icon name="flower" size="64rpx" />
            <text>订花</text>
        </view>
        <view class="feature-item">
            <t-icon name="shop" size="64rpx" />
            <text>周边商城</text>
        </view>
    </view>
    <!-- 功能按钮占位 -->
    <view class="feature-buttons">
        <view class="feature-item">
            <t-icon name="flower" size="64rpx" />
            <text>订花</text>
        </view>
        <view class="feature-item">
            <t-icon name="shop" size="64rpx" />
            <text>周边商城</text>
        </view>
    </view>
    <!-- 功能按钮占位 -->
    <view class="feature-buttons">
        <view class="feature-item">
            <t-icon name="flower" size="64rpx" />
            <text>订花</text>
        </view>
        <view class="feature-item">
            <t-icon name="shop" size="64rpx" />
            <text>周边商城</text>
        </view>
    </view>
</view>